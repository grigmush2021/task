# Task

## Installation
1. Clone this repository
2. Copy .env.example to .env and make your configurations
3. Run `composer install/update`
4. Run `php artisan migrate --seed`
5. Run `php artisan passport:install`
